const nextConfig = {
  i18n: {
    locales: ['kn'],
    defaultLocale: 'kn',
    localeDetection: false, // Disable detection for single language instance
  },
  trailingSlash: false,
};
module.exports = nextConfig;
