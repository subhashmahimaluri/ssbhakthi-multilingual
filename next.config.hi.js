const nextConfig = {
  i18n: {
    locales: ['hi'],
    defaultLocale: 'hi',
    localeDetection: false, // Disable detection for single language instance
  },
  trailingSlash: false,
};
module.exports = nextConfig;
